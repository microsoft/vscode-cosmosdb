// Enter these values to enable postgres account view
export const config = {
    user: '<username>@<postgres_account_name>', //Add username@accountname here, username is the Azure username
    password: '<password>', //Add the password created for the Postgres Account
    ssl: false //Adjust this value based on your account settings
};
