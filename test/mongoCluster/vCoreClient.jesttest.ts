/// <reference types="jest" />

import { VCoreClient } from '../../src/vCore/VCoreClient';

describe('vCoreClient', () => {
    it('should be defined', () => {
        expect(VCoreClient).toBeDefined();
    });


});
